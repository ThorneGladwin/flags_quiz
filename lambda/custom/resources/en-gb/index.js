const { CoreIntents } = require("./coreIntents");
const { HelloWorldIntent } = require("./helloWorldIntent");

const speech = {
    CoreIntents,
    HelloWorldIntent
};

module.exports = {
    speech
};