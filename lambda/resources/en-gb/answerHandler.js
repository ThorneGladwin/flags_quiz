const AnswerHandler = {
  Results: (score) => `You got ${score} out of 5. If you want to have another go, just say "Play again". Otherwise, just say "Stop".`
};

module.exports = {
  AnswerHandler
};
